[toolchain]
channel = "stable"
# Configuration multi-plateforme pour compilation native
# Linux: compile automatiquement pour x86_64-unknown-linux-gnu
# Windows: compile automatiquement pour x86_64-pc-windows-gnu (MinGW) si installé, sinon MSVC
# Note: Sur Windows, si vous rencontrez des erreurs, installez MinGW-w64 via MSYS2 ou Chocolatey
